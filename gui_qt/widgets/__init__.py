"""Reusable PyQt6 widgets for the randomizer GUI.

Every module under this package imports PyQt6 at the top; the parent
package (:mod:`gui_qt`) keeps PyQt6 optional, so callers that need to
avoid the import must not touch this sub-package directly.
"""
